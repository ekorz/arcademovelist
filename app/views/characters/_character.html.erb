<p><strong><%= link_to character.name, character_path(character.id) %></strong> <%= character.description %> <%= link_to "Edit", edit_character_path(character.id) %> <%= link_to "Delete", character_path(character.id), method: :delete, data: {confirm: "Sure?" } %> </p>