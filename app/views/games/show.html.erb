<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @game.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @game.description %>
</p>

<p>
  <strong>Year:</strong>
  <%= @game.year %>
</p>

<div id="characters_wrapper">
  <%= render @game.characters %>
</div>


<%= link_to 'New Character', new_character_path(character: {game_id: @game.id}) %>

<%= link_to 'Edit', edit_game_path(@game) %> |
<%= link_to 'Back', games_path %>
