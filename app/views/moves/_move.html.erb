<p><%= link_to move.name, move_path(move.id) %> <%= move.description %> 
<%= link_to "Edit", edit_move_path(move.id) %> <%= link_to "Delete", move_path(move.id), method: :delete, data: {confirm: "Sure?" } %> </p>