<%= form_for([@character, @character.moves.build]) do |f| %>
  <%= f.text_field :name, placeholder: "Name" %>
  <%= f.text_field :description, placeholder: "Description" %>
  <%= f.submit %>
<% end %>